{% extends extends_from %}

{% block admin_body_content %}
    <div class="row">
        <div class="col-xs-12">
            <div class="box box-default">
                <div class="box-header with-border"><h3 class="box-title">Menüeinträge bearbeiten</h3></div>
                <div class="box-body">
                    {% block admin_content %}
                        <div class="admin-menu-form">
                            <div class="form-main">{{ form(form) }}</div>
                        </div>
                    {% endblock %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

